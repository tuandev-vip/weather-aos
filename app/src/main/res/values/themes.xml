<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.WeatherWorld" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/weather_splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.WeatherWorld.Main</item>
    </style>

    <style name="Theme.WeatherWorld.Main" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/weather_splash_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
